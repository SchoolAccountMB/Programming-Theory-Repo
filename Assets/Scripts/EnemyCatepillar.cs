using UnityEngine;

public class EnemyCatepillar : Enemy
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Spawn();
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    protected override void Move()
    {
        // Does Nothing
    }
}
